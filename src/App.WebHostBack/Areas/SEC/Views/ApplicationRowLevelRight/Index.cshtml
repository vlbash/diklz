@{
    ViewData["Title"] = "Реєстр прав на рівні записів";
}

@section Buttons {
    <a class="btn btn-round icon-add"
       asp-action="Edit"
       data-tooltipleft='1'
       data-title="Додати новий AppRLR">
    </a>
}

<div class="content-search-wrapper">
    <partial name="_Searchform" />
</div>

<div class="results" id="results">

</div>

@section Scripts {

    <script>
        var searchFormControls = {
            controlsConfig: [
                {
                    labelName: 'Назва',
                    name: 'EntityName',
                    type: 'text'
                }
            ]
        }

        var searchFormConfig = {
            formConfig: {
                formName: 'form0',
                formDataAjaxUpdate: '#results',
                // formDataAjax: 'true',
                // formDataAjaxMode: 'replace',
                formMethod: 'post',
                controls:searchFormControls
            },
            presetFiltersUrl:{
            },

            mainInputConfig: {
                labelName: 'Назва',
                type: 'text',
                mainInputLookUp: ['EntityName']
            }

        };

        mt.createContentSearchForm(searchFormConfig);

    </script>

}
